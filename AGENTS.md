# AGENTS.md

本文件记录项目负责人的长期工作偏好，任何接手的 AI 应先读此文件，再读 `00-control/PROJECT_HUB.md`。

## 版本控制偏好

1. **自动提交**：每完成一个独立工作单元（一次 Run、一组文档更新、一个功能），直接用 git commit 提交，不必逐次询问。提交信息用英文祈使句，风格参照现有历史（如 `Implement Pipeline 0.2.0 candidate-first identity flow`）。
2. **不推送、不打标签**：除非负责人明确要求，只提交到本地仓库。
3. **禁止修改的对象**：`data/baselines/` 下已验收的 baseline 文件；已 accept 的 Pipeline 响应 JSON。发现它们需要改动时，停下来请示。
4. 受限原文（Git 忽略目录中的现代版权材料）永远不提交、不复制到可跟踪路径。

## 沟通偏好

1. 负责人非技术背景，解释时用大白话，避免未解释的缩写和黑话。
2. 决策点用结构化的方式呈现（选项 + 影响），不要让负责人从长文中找"我该做什么"。
3. 文档写了不等于功能做了；汇报进度时区分"已验收 / 有产物未验收 / 只有设计"。
