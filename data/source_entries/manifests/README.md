# Source Entry 清单 / Source Entry Manifests

每个 Source Entry 保存一个进入 Git 的 Manifest。Manifest 包含定位信息、哈希、版权元数据和存储状态，但不包含受限全文。

Store one tracked Manifest per Source Entry. Manifests contain locators, hashes, rights metadata, and storage status, but no restricted full text.
